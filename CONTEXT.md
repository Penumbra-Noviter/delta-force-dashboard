# 收益计算器 · 领域词汇表

单一上下文。仅作术语消歧，不含实现细节。

## 实体

- **记录** — 某一日期的现金与仓库价值。
- **现金** — 单日记录中的现金部分。
- **仓库** — 仓库价值，**已含现金**。总收益 = 仓库价值。
- **较前日** — 当日仓库值对比「前一有记录日」仓库值的派生关系；是差值、收益率、盈亏三种展示的共同输入。

## 不变式

- **现金 ⊆ 仓库** — 现金是仓库价值的组成部分，`现金 > 仓库` 为非法状态。业务层保留历史异常数据（仅告警），新写入由 UI 层拦截。

## 计算语义

- **总收益** — 最近仓库值（仓库已含现金），**不是** cash + warehouse。
- **收益率** — 较前日仓库值的百分比变化；无前值或前值为零时**不存在**（显示 —）。
- **盈亏** — 较前日的方向判定：盈 / 亏 / 平。
- **录入条数基准** — 保留、汇总、展示均以「实际录入记录条数」（最近 7 条）为基准，而非日历天数；间断录入不丢记录。
- **序列化** — 记录在内存（`DayRecord`）与磁盘（`{"cash","warehouse"}` dict）之间的转换；只读数据（logic.data）→ 可写副本（serialize 产物）单向流动，logic 与磁盘不共享内存。

## 校验

- **有效性** — 单字段文本的合法性（合法/非法/空），驱动保存按钮启停。
- **跨字段校验** — 现金⊆仓库不变式的实时检查，挂在单字段有效性之后联动两框红框。

## 展示规则

- **趋势信号** — 方向性信号（正 / 负 / 平 / 无），业务层产出，UI 层映射颜色。零值 = 平。
- **带符号金额** — 金额 + 符号 + 方向信号 的组合展示（如 `+¥1,234.56`），较前日差值、汇总共用。零值无前缀。
- **格式化** — 金额在输入框的展示形式：聚焦反格式化（可编辑纯数字）、失焦/复用填值格式化（带货币符号）。
